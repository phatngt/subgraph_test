{
  "name": "subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraph",
    "create-local": "graph create --node http://127.0.0.1:8020/ subgraph",
    "remove-local": "graph remove --node http://127.0.0.1:8020/ subgraph",
    "deploy-local": "graph deploy --node http://127.0.0.1:8020/ --ipfs http://localhost:5001 subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
